server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JuanmiCk7/bootCamp-ConfigServer.git
          default-label: main
          search-paths:
          - 'reto-config-service.yaml'
          